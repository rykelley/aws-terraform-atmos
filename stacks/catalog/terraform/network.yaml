components:
  terraform:
    network:
      metadata:
        component: network
      vars:
        name: vnet
        attributes: []
        vnet_address_space:
          - "10.0.0.0/16"
        aks_subnet_prefix: "10.0.1.0/24"
